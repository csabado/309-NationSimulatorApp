package com.example.tabbedversion;

import com.example.tabbedversion.data.model.LoggedInUser;
import com.example.tabbedversion.login.LoggedInUserView;
import com.example.tabbedversion.login.LoginResult;
import com.example.tabbedversion.login.LoginViewModel;


import org.mockito.internal.matchers.InstanceOf;
import org.mockito.runners.MockitoJUnitRunner;
import org.mockito.Mock;
import org.mockito.runners.MockitoJUnitRunner;
import org.mockito.junit.MockitoJUnit;
import org.mockito.junit.MockitoRule;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;
import static org.mockito.Mockito.mock;


/*RunWith(MockitoJUnitRunner.class)
public class mockitoTest {
    @Test
    public void testLoginSuccess() {

        LoginResult test = mock(LoginResult.class);
        MainActivity testLogin = new MainActivity();
        Integer error;

        Assert.assertThat(test.getError(), new InstanceOf(Integer.class));
    }



}
*/